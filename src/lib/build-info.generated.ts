// Gerado/sobrescrito por scripts/build.mjs antes de cada `vite build`.
// O valor abaixo só é usado em desenvolvimento (`vite dev`), onde o build
// script não roda.
export const BUILD_TIME = "dev";
