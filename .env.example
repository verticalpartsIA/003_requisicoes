VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
PORT=3000
HOST=0.0.0.0
APP_ORIGIN=http://localhost:3000
DEFAULT_REQUESTER_NAME=Operador VerticalParts
DEFAULT_REQUESTER_EMAIL=operador@verticalparts.com.br
DEFAULT_REQUESTER_DEPARTMENT=Compras

# Chave compartilhada com a edge function pública `track-activity` do portal
# central (vpsistema) — usada para autenticar os eventos de entrada/saída da
# timeline de rastro de acesso cross-sistema. Configurar no ambiente de
# deploy com o valor combinado fora do código (NUNCA commitar o valor real).
VITE_TRACK_ACTIVITY_KEY=
